/**
 * Base/Controller
 */

;(function(global, $, undefined){
  'use strict';

  var Controller = global.Controller = Base.extend({
    // @TODO methods and properties
  });

  return Controller;

})(window, jQuery);