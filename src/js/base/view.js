/**
 * Base/View
 */

;(function(global, $, Base, undefined){
  'use strict';

  var View = global.View = Base.extend({
    // @TODO methods and properties
  });

  return View;

})(window, jQuery, Base);